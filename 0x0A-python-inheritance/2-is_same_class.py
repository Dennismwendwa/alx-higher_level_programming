#!/usr/bin/python3
"""checking if is instance of class"""


def is_same_class(obj, a_class):
    """this method checkis if is instance"""
    
    if type(obj) == a_class:
        return True
    else:
        return False
